# ─────────────────────────────────────────────────────────────────────
# Enquiry form email delivery  (src/app/api/enquiry/route.ts)
# ─────────────────────────────────────────────────────────────────────
#
# Copy this file to `.env.local` and fill it in. `.env.local` is
# gitignored — never commit real keys.
#
# Until RESEND_API_KEY and ENQUIRY_FROM_EMAIL are both set, the API route
# returns 503 and the enquiry form falls back to sending via WhatsApp. The
# site works either way; it just doesn't email until this is configured.

# From https://resend.com/api-keys — starts with "re_".
RESEND_API_KEY=

# The address enquiries are sent FROM.
#
# ⚠ This must be on a domain verified in Resend (Domains → Add Domain, then
# add the DKIM/SPF records to the domain's DNS). It is NOT the clinic's
# mailbox — it is the sending identity. Using an unverified domain, or a
# free provider address like @gmail.com, is the usual reason enquiries land
# in spam or are rejected outright.
#
# e.g. no-reply@healthylook-aesthetic.com
ENQUIRY_FROM_EMAIL=

# The mailbox enquiries are delivered TO. Optional — defaults to EMAIL in
# src/lib/constants.ts (info@healthylook-aesthetic.com).
ENQUIRY_TO_EMAIL=
